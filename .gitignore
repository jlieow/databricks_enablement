__pycache__/
*.pyc
.DS_Store
**/.claude
**/.isaac
**/.databricks

# Databricks Asset Bundle build artifacts
**/bundle.zip

# Terraform build artifacts and state. terraform.tfvars can hold real secret
# values and tfstate can hold them in plaintext, so never commit either.
**/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars